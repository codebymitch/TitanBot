import { SlashCommandBuilder, MessageFlags } from 'discord.js'; import { createEmbed } from '../../utils/embeds.js'; import { levelKey } from '../../modules/community/store.js'; import { requireAccess,AccessLevel } from '../../modules/community/permissions.js';
export function levelCommand(name){const data=new SlashCommandBuilder().setName(name).setDescription(`EditIL ${name}`).setDMPermission(false);if(name==='setxp')data.addIntegerOption(o=>o.setName('xp').setDescription('XP amount').setRequired(true).setMinValue(0).setMaxValue(100000000));if(name!=='leaderboard')data.addUserOption(o=>o.setName('member').setDescription('Member'));return{data,async execute(i,c){const admin=['setxp','resetxp'].includes(name);if(!await requireAccess(i,c,admin?AccessLevel.ADMIN:AccessLevel.VERIFIED))return;if(name==='leaderboard'){const keys=await c.db.list(`community:${i.guildId}:level:`);const rows=await Promise.all((keys||[]).map(async key=>({id:key.split(':').pop(),...(await c.db.get(key,{xp:0,level:0}))})));const board=rows.sort((a,b)=>(b.xp||0)-(a.xp||0)).slice(0,10);return i.reply({embeds:[createEmbed({title:'טבלת המובילים',description:board.length?board.map((v,n)=>`**${n+1}.** <@${v.id}> — ${v.xp||0} XP (רמה ${v.level||0})`).join('\n'):'עדיין אין נתוני רמות.',color:'primary'})]});}const user=i.options.getUser('member')||i.user,key=levelKey(i.guildId,user.id);if(name==='setxp'){const xp=i.options.getInteger('xp');await c.db.set(key,{xp,level:Math.floor(Math.sqrt(xp/100)),last:0});return i.reply({content:`ה-XP של ${user} עודכן ל-${xp}.`,flags:MessageFlags.Ephemeral});}if(name==='resetxp'){await c.db.delete(key);return i.reply({content:`נתוני הרמה של ${user} אופסו.`,flags:MessageFlags.Ephemeral});}const v=await c.db.get(key,{xp:0,level:0});await i.reply({embeds:[createEmbed({title:name==='profile'?'פרופיל עורך':'דירוג',description:`${user}\nרמה: **${v.level||0}**\nניסיון: **${v.xp||0} XP**`,color:'primary'}).setThumbnail(user.displayAvatarURL())]});}}}
