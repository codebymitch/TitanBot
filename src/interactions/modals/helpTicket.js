/**
 * Help Ticket Modal Interactions
 * 
 * Exports the modal handlers for the support ticket system.
 */

import { helpCloseModalHandler } from '../../handlers/helpTicketButtons.js';

export default [
  helpCloseModalHandler,
];